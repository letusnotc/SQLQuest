# Screenshots

Add these 7 images here (exact filenames) — they're referenced by the main README, in this order:

1. `world.png` — the jungle game world we built (used as the banner)
2. `world-progress.png` — the per-world progress page
3. `dashboard.png` — "Your Worlds" dashboard with the upload button and world cards
4. `profile.png` — the profile page
5. `badges.png` — the badges page
6. `leaderboard.png` — the global leaderboard
7. `level.png` — a level page (Monaco editor + data preview)
8. `level-analysis.png` — the analysis page (side-by-side, runtimes, EXPLAIN, optimization)
9. `sage.png` — the "Ask Sage" AI tutor drawer
